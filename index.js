/**
 * @format
 */

import { AppRegistry } from 'react-native'

import Screens from '@screens'

import { name as appName } from './app.json'

AppRegistry.registerComponent(appName, () => Screens.App)
